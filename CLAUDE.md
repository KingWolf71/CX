1. don't use command line compiler as it won't work; I can compile manually via IDE
2. always leave definitions at beginning of procedure - no exceptions

3. No procedure static variables; we use global variables which can be reset between runs
